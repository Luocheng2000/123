#include <stdio.h>
int main(void)
{
    int i, n, x ;
    printf("Enter n:\n");
    scanf("%d", &n);
    while (n<=0)
    {
     
    }
    sum += x;
    printf("sum = %.5lf\n", sum);
    return 0;
}
